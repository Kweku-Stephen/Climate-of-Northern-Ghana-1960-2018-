#########

require(tidyverse)
pprr <- sj
head(pprr)
c.60.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="60 01")$Prcp, na.rm = T)
c.61.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="61 01")$Prcp, na.rm = T)
c.62.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="62 01")$Prcp, na.rm = T)
c.63.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="63 01")$Prcp, na.rm = T)
c.64.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="64 01")$Prcp, na.rm = T)
c.65.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="65 01")$Prcp, na.rm = T)
c.66.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="66 01")$Prcp, na.rm = T)
c.67.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="67 01")$Prcp, na.rm = T)
c.68.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="68 01")$Prcp, na.rm = T)
c.69.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="69 01")$Prcp, na.rm = T)
c.70.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="70 01")$Prcp, na.rm = T)
c.71.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="71 01")$Prcp, na.rm = T)
c.72.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="72 01")$Prcp, na.rm = T)
c.73.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="73 01")$Prcp, na.rm = T)
c.74.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="74 01")$Prcp, na.rm = T)
c.75.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="75 01")$Prcp, na.rm = T)
c.76.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="76 01")$Prcp, na.rm = T)
c.77.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="77 01")$Prcp, na.rm = T)
c.78.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="78 01")$Prcp, na.rm = T)
c.79.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="79 01")$Prcp, na.rm = T)
c.80.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="80 01")$Prcp, na.rm = T)
c.81.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="81 01")$Prcp, na.rm = T)
c.82.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="82 01")$Prcp, na.rm = T)
c.83.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="83 01")$Prcp, na.rm = T)
c.84.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="84 01")$Prcp, na.rm = T)
c.85.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="85 01")$Prcp, na.rm = T)
c.86.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="86 01")$Prcp, na.rm = T)
c.87.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="87 01")$Prcp, na.rm = T)
c.88.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="88 01")$Prcp, na.rm = T)
c.89.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="89 01")$Prcp, na.rm = T)
c.90.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="90 01")$Prcp, na.rm = T)
c.91.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="91 01")$Prcp, na.rm = T)
c.92.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="92 01")$Prcp, na.rm = T)
c.93.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="93 01")$Prcp, na.rm = T)
c.94.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="94 01")$Prcp, na.rm = T)
c.95.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="95 01")$Prcp, na.rm = T)
c.96.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="96 01")$Prcp, na.rm = T)
c.97.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="97 01")$Prcp, na.rm = T)
c.98.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="98 01")$Prcp, na.rm = T)
c.99.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="99 01")$Prcp, na.rm = T)
c.00.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="00 01")$Prcp, na.rm = T)
c.01.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="01 01")$Prcp, na.rm = T)
c.02.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="02 01")$Prcp, na.rm = T)
c.03.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="03 01")$Prcp, na.rm = T)
c.04.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="04 01")$Prcp, na.rm = T)
c.05.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="05 01")$Prcp, na.rm = T)
c.06.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="06 01")$Prcp, na.rm = T)
c.07.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="07 01")$Prcp, na.rm = T)
c.08.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="08 01")$Prcp, na.rm = T)
c.09.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="09 01")$Prcp, na.rm = T)
c.10.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="10 01")$Prcp, na.rm = T)
c.11.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="11 01")$Prcp, na.rm = T)
c.12.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="12 01")$Prcp, na.rm = T)
c.13.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="13 01")$Prcp, na.rm = T)
c.14.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="14 01")$Prcp, na.rm = T)
c.15.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="15 01")$Prcp, na.rm = T)
c.16.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="16 01")$Prcp, na.rm = T)
#c.17.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="17 01")$Prcp, na.rm = T)
#c.18.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="18 01")$Prcp, na.rm = T)
#c.19.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="19 01")$Prcp, na.rm = T)



#vectorization
Janv <- c(c.60.1, c.61.1,c.62.1,c.63.1,c.64.1,c.65.1,c.66.1,c.67.1,c.68.1,c.69.1,c.70.1,
          c.71.1,c.72.1,c.73.1,c.74.1,c.75.1,c.76.1,c.77.1,c.78.1,c.79.1,c.80.1,c.81.1,c.82.1,
          c.83.1,c.84.1,c.85.1,c.86.1,c.87.1,c.88.1,c.89.1,c.90.1,c.91.1,c.92.1,c.93.1,c.94.1,
          c.95.1,c.96.1,c.97.1,c.98.1,c.99.1,c.00.1,c.01.1,c.02.1,c.03.1,c.04.1,c.05.1,c.06.1,
          c.07.1,c.08.1,c.09.1,c.10.1,c.11.1,c.12.1,c.13.1,c.14.1,c.15.1,c.16.1)

write.csv(
  data.frame(Prcp.df.ann[,1], Janv),
  file="Jan_data_across.csv"
)

ggplot(data=data.frame(Prcp.df.ann[,1], Janv), aes(x=Prcp.df.ann[,1], y=Janv)) + 
  geom_line(col="darkblue", lwd=2) + scale_x_date(date_breaks = "4 years",
                                                  date_labels = "%Y") +
  labs(title = "Seasonal Cycle of January (1960-2016)\n Tamale",
       x="Year",y="Rainfall (mm)") +
  theme(plot.title = element_text(size=18, hjust = 0.5, face = "bold"),
        axis.text = element_text(size = 13),
        axis.title = element_text(size = 13))
dev.copy(png, filename="Seasonal Cycle of January (1960-2016) of Tamale.png",width=1000,
         height=750)
dev.off()


#plot(seq(as.Date("1960-01-01"), by="year", length.out=length(1960:2016)), 
#     Janv, type="l", col="darkblue", lwd=5)


#let,
pprr <- sf
head(pprr)
f.60.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="60 02")$Prcp, na.rm = T)
f.61.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="61 02")$Prcp, na.rm = T)
f.62.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="62 02")$Prcp, na.rm = T)
f.63.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="63 02")$Prcp, na.rm = T)
f.64.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="64 02")$Prcp, na.rm = T)
f.65.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="65 02")$Prcp, na.rm = T)
f.66.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="66 02")$Prcp, na.rm = T)
f.67.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="67 02")$Prcp, na.rm = T)
f.68.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="68 02")$Prcp, na.rm = T)
f.69.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="69 02")$Prcp, na.rm = T)
f.70.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="70 02")$Prcp, na.rm = T)
f.71.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="71 02")$Prcp, na.rm = T)
f.72.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="72 02")$Prcp, na.rm = T)
f.73.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="73 02")$Prcp, na.rm = T)
f.74.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="74 02")$Prcp, na.rm = T)
f.75.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="75 02")$Prcp, na.rm = T)
f.76.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="76 02")$Prcp, na.rm = T)
f.77.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="77 02")$Prcp, na.rm = T)
f.78.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="78 02")$Prcp, na.rm = T)
f.79.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="79 02")$Prcp, na.rm = T)
f.80.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="80 02")$Prcp, na.rm = T)
f.81.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="81 02")$Prcp, na.rm = T)
f.82.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="82 02")$Prcp, na.rm = T)
f.83.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="83 02")$Prcp, na.rm = T)
f.84.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="84 02")$Prcp, na.rm = T)
f.85.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="85 02")$Prcp, na.rm = T)
f.86.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="86 02")$Prcp, na.rm = T)
f.87.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="87 02")$Prcp, na.rm = T)
f.88.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="88 02")$Prcp, na.rm = T)
f.89.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="89 02")$Prcp, na.rm = T)
f.90.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="90 02")$Prcp, na.rm = T)
f.91.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="91 02")$Prcp, na.rm = T)
f.92.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="92 02")$Prcp, na.rm = T)
f.93.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="93 02")$Prcp, na.rm = T)
f.94.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="94 02")$Prcp, na.rm = T)
f.95.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="95 02")$Prcp, na.rm = T)
f.96.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="96 02")$Prcp, na.rm = T)
f.97.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="97 02")$Prcp, na.rm = T)
f.98.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="98 02")$Prcp, na.rm = T)
f.99.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="99 02")$Prcp, na.rm = T)
f.00.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="00 02")$Prcp, na.rm = T)
f.01.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="01 02")$Prcp, na.rm = T)
f.02.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="02 02")$Prcp, na.rm = T)
f.03.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="03 02")$Prcp, na.rm = T)
f.04.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="04 02")$Prcp, na.rm = T)
f.05.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="05 02")$Prcp, na.rm = T)
f.06.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="06 02")$Prcp, na.rm = T)
f.07.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="07 02")$Prcp, na.rm = T)
f.08.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="08 02")$Prcp, na.rm = T)
f.09.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="09 02")$Prcp, na.rm = T)
f.10.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="10 02")$Prcp, na.rm = T)
f.11.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="11 02")$Prcp, na.rm = T)
f.12.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="12 02")$Prcp, na.rm = T)
f.13.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="13 02")$Prcp, na.rm = T)
f.14.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="14 02")$Prcp, na.rm = T)
f.15.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="15 02")$Prcp, na.rm = T)
f.16.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="16 02")$Prcp, na.rm = T)
#f.17.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="17 02")$Prcp, na.rm = T)
#f.18.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="18 01")$Prcp, na.rm = T)
#c.19.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="19 01")$Prcp, na.rm = T)

Febv <- c(f.60.1, f.61.1,f.62.1,f.63.1,f.64.1,f.65.1,f.66.1,f.67.1,f.68.1,f.69.1,f.70.1,
          f.71.1,f.72.1,f.73.1,f.74.1,f.75.1,f.76.1,f.77.1,f.78.1,f.79.1,f.80.1,f.81.1,f.82.1,
          f.83.1,f.84.1,f.85.1,f.86.1,f.87.1,f.88.1,f.89.1,f.90.1,f.91.1,f.92.1,f.93.1,f.94.1,
          f.95.1,f.96.1,f.97.1,f.98.1,f.99.1,f.00.1,f.01.1,f.02.1,f.03.1,f.04.1,f.05.1,f.06.1,
          f.07.1,f.08.1,f.09.1,f.10.1,f.11.1,f.12.1,f.13.1,f.14.1,f.15.1,f.16.1)

ggplot(data=data.frame(Prcp.df.ann[,1], Febv), aes(x=Prcp.df.ann[,1], y=Febv)) + 
  geom_line(col="darkblue", lwd=2) + scale_x_date(date_breaks = "4 years",
                                                  date_labels = "%Y") +
  labs(title = "Seasonal Cycle of February (1960-2016)\n Tamale",
       x="Year",y="Rainfall (mm)") +
  theme(plot.title = element_text(size=18, hjust = 0.5, face = "bold"),
        axis.text = element_text(size = 13),
        axis.title = element_text(size = 13))
dev.copy(png, filename="Seasonal Cycle of February (1960-2016) of Tamale.png",
         width=1000, height=750)
dev.off()


#plot(seq(as.Date("1960-01-01"), by="year", length.out=length(1960:2016)), 
#           Febv, type="l", col="darkblue", lwd=5)


#let,
pprr <- sm
head(pprr)
m.60.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="60 03")$Prcp, na.rm = T)
m.61.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="61 03")$Prcp, na.rm = T)
m.62.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="62 03")$Prcp, na.rm = T)
m.63.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="63 03")$Prcp, na.rm = T)
m.64.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="64 03")$Prcp, na.rm = T)
m.65.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="65 03")$Prcp, na.rm = T)
m.66.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="66 03")$Prcp, na.rm = T)
m.67.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="67 03")$Prcp, na.rm = T)
m.68.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="68 03")$Prcp, na.rm = T)
m.69.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="69 03")$Prcp, na.rm = T)
m.70.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="70 03")$Prcp, na.rm = T)
m.71.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="71 03")$Prcp, na.rm = T)
m.72.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="72 03")$Prcp, na.rm = T)
m.73.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="73 03")$Prcp, na.rm = T)
m.74.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="74 03")$Prcp, na.rm = T)
m.75.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="75 03")$Prcp, na.rm = T)
m.76.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="76 03")$Prcp, na.rm = T)
m.77.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="77 03")$Prcp, na.rm = T)
m.78.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="78 03")$Prcp, na.rm = T)
m.79.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="79 03")$Prcp, na.rm = T)
m.80.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="80 03")$Prcp, na.rm = T)
m.81.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="81 03")$Prcp, na.rm = T)
m.82.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="82 03")$Prcp, na.rm = T)
m.83.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="83 03")$Prcp, na.rm = T)
m.84.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="84 03")$Prcp, na.rm = T)
m.85.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="85 03")$Prcp, na.rm = T)
m.86.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="86 03")$Prcp, na.rm = T)
m.87.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="87 03")$Prcp, na.rm = T)
m.88.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="88 03")$Prcp, na.rm = T)
m.89.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="89 03")$Prcp, na.rm = T)
m.90.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="90 03")$Prcp, na.rm = T)
m.91.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="91 03")$Prcp, na.rm = T)
m.92.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="92 03")$Prcp, na.rm = T)
m.93.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="93 03")$Prcp, na.rm = T)
m.94.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="94 03")$Prcp, na.rm = T)
m.95.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="95 03")$Prcp, na.rm = T)
m.96.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="96 03")$Prcp, na.rm = T)
m.97.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="97 03")$Prcp, na.rm = T)
m.98.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="98 03")$Prcp, na.rm = T)
m.99.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="99 03")$Prcp, na.rm = T)
m.00.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="00 03")$Prcp, na.rm = T)
m.01.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="01 03")$Prcp, na.rm = T)
m.02.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="02 03")$Prcp, na.rm = T)
m.03.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="03 03")$Prcp, na.rm = T)
m.04.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="04 03")$Prcp, na.rm = T)
m.05.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="05 03")$Prcp, na.rm = T)
m.06.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="06 03")$Prcp, na.rm = T)
m.07.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="07 03")$Prcp, na.rm = T)
m.08.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="08 03")$Prcp, na.rm = T)
m.09.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="09 03")$Prcp, na.rm = T)
m.10.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="10 03")$Prcp, na.rm = T)
m.11.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="11 03")$Prcp, na.rm = T)
m.12.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="12 03")$Prcp, na.rm = T)
m.13.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="13 03")$Prcp, na.rm = T)
m.14.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="14 03")$Prcp, na.rm = T)
m.15.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="15 03")$Prcp, na.rm = T)
m.16.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="16 03")$Prcp, na.rm = T)
#f.17.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="17 02")$Prcp, na.rm = T)
#f.18.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="18 01")$Prcp, na.rm = T)
#c.19.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="19 01")$Prcp, na.rm = T)

Marv <- c(m.60.1,m.61.1,m.62.1,m.63.1,m.64.1,m.65.1,m.66.1,m.67.1,m.68.1,m.69.1,m.70.1,
          m.71.1,m.72.1,m.73.1,m.74.1,m.75.1,m.76.1,m.77.1,m.78.1,m.79.1,m.80.1,m.81.1,m.82.1,
          m.83.1,m.84.1,m.85.1,m.86.1,m.87.1,m.88.1,m.89.1,m.90.1,m.91.1,m.92.1,m.93.1,m.94.1,
          m.95.1,m.96.1,m.97.1,m.98.1,m.99.1,m.00.1,m.01.1,m.02.1,m.03.1,m.04.1,m.05.1,m.06.1,
          m.07.1,m.08.1,m.09.1,m.10.1,m.11.1,m.12.1,m.13.1,m.14.1,m.15.1,m.16.1)

ggplot(data=data.frame(Prcp.df.ann[,1], Marv), aes(x=Prcp.df.ann[,1], y=Marv)) + 
  geom_line(col="darkblue", lwd=2) + scale_x_date(date_breaks = "4 years",
                                                  date_labels = "%Y") +
  labs(title = "Seasonal Cycle of March (1960-2016)\n Tamale",
       x="Year",y="Rainfall (mm)") +
  theme(plot.title = element_text(size=18, hjust = 0.5, face = "bold"),
        axis.text = element_text(size = 13),
        axis.title = element_text(size = 13))
dev.copy(png, filename="Seasonal Cycle of March (1960-2016) of Tamale.png",
         width=1000, height=750)
dev.off()


#let,
pprr <- sa
head(pprr)
a.60.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="60 04")$Prcp, na.rm = T)
a.61.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="61 04")$Prcp, na.rm = T)
a.62.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="62 04")$Prcp, na.rm = T)
a.63.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="63 04")$Prcp, na.rm = T)
a.64.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="64 04")$Prcp, na.rm = T)
a.65.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="65 04")$Prcp, na.rm = T)
a.66.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="66 04")$Prcp, na.rm = T)
a.67.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="67 04")$Prcp, na.rm = T)
a.68.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="68 04")$Prcp, na.rm = T)
a.69.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="69 04")$Prcp, na.rm = T)
a.70.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="70 04")$Prcp, na.rm = T)
a.71.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="71 04")$Prcp, na.rm = T)
a.72.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="72 04")$Prcp, na.rm = T)
a.73.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="73 04")$Prcp, na.rm = T)
a.74.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="74 04")$Prcp, na.rm = T)
a.75.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="75 04")$Prcp, na.rm = T)
a.76.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="76 04")$Prcp, na.rm = T)
a.77.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="77 04")$Prcp, na.rm = T)
a.78.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="78 04")$Prcp, na.rm = T)
a.79.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="79 04")$Prcp, na.rm = T)
a.80.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="80 04")$Prcp, na.rm = T)
a.81.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="81 04")$Prcp, na.rm = T)
a.82.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="82 04")$Prcp, na.rm = T)
a.83.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="83 04")$Prcp, na.rm = T)
a.84.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="84 04")$Prcp, na.rm = T)
a.85.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="85 04")$Prcp, na.rm = T)
a.86.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="86 04")$Prcp, na.rm = T)
a.87.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="87 04")$Prcp, na.rm = T)
a.88.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="88 04")$Prcp, na.rm = T)
a.89.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="89 04")$Prcp, na.rm = T)
a.90.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="90 04")$Prcp, na.rm = T)
a.91.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="91 04")$Prcp, na.rm = T)
a.92.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="92 04")$Prcp, na.rm = T)
a.93.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="93 04")$Prcp, na.rm = T)
a.94.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="94 04")$Prcp, na.rm = T)
a.95.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="95 04")$Prcp, na.rm = T)
a.96.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="96 04")$Prcp, na.rm = T)
a.97.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="97 04")$Prcp, na.rm = T)
a.98.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="98 04")$Prcp, na.rm = T)
a.99.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="99 04")$Prcp, na.rm = T)
a.00.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="00 04")$Prcp, na.rm = T)
a.01.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="01 04")$Prcp, na.rm = T)
a.02.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="02 04")$Prcp, na.rm = T)
a.03.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="03 04")$Prcp, na.rm = T)
a.04.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="04 04")$Prcp, na.rm = T)
a.05.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="05 04")$Prcp, na.rm = T)
a.06.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="06 04")$Prcp, na.rm = T)
a.07.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="07 04")$Prcp, na.rm = T)
a.08.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="08 04")$Prcp, na.rm = T)
a.09.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="09 04")$Prcp, na.rm = T)
a.10.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="10 04")$Prcp, na.rm = T)
a.11.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="11 04")$Prcp, na.rm = T)
a.12.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="12 04")$Prcp, na.rm = T)
a.13.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="13 04")$Prcp, na.rm = T)
a.14.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="14 04")$Prcp, na.rm = T)
a.15.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="15 04")$Prcp, na.rm = T)
a.16.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="16 04")$Prcp, na.rm = T)
#f.17.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="17 02")$Prcp, na.rm = T)
#f.18.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="18 01")$Prcp, na.rm = T)
#c.19.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="19 01")$Prcp, na.rm = T)

Aprv <- c(a.60.1,a.61.1,a.62.1,a.63.1,a.64.1,a.65.1,a.66.1,a.67.1,a.68.1,a.69.1,a.70.1,
          a.71.1,a.72.1,a.73.1,a.74.1,a.75.1,a.76.1,a.77.1,a.78.1,a.79.1,a.80.1,a.81.1,a.82.1,
          a.83.1,a.84.1,a.85.1,a.86.1,a.87.1,a.88.1,a.89.1,a.90.1,a.91.1,a.92.1,a.93.1,a.94.1,
          a.95.1,a.96.1,a.97.1,a.98.1,a.99.1,a.00.1,a.01.1,a.02.1,a.03.1,a.04.1,a.05.1,a.06.1,
          a.07.1,a.08.1,a.09.1,a.10.1,a.11.1,a.12.1,a.13.1,a.14.1,a.15.1,a.16.1)

ggplot(data=data.frame(Prcp.df.ann[,1], Aprv), aes(x=Prcp.df.ann[,1], y=Aprv)) + 
  geom_line(col="darkblue", lwd=2) + scale_x_date(date_breaks = "4 years",
                                                  date_labels = "%Y") +
  labs(title = "Seasonal Cycle of April (1960-2016)\n Tamale",
       x="Year",y="Rainfall (mm)") +
  theme(plot.title = element_text(size=18, hjust = 0.5, face = "bold"),
        axis.text = element_text(size = 13),
        axis.title = element_text(size = 13))
dev.copy(png, filename="Seasonal Cycle of April (1960-2016) of Tamale.png", width=1000,
         height=750)
dev.off()


#let,
pprr <- sma
head(pprr)
ma.60.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="60 05")$Prcp, na.rm = T)
ma.61.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="61 05")$Prcp, na.rm = T)
ma.62.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="62 05")$Prcp, na.rm = T)
ma.63.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="63 05")$Prcp, na.rm = T)
ma.64.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="64 05")$Prcp, na.rm = T)
ma.65.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="65 05")$Prcp, na.rm = T)
ma.66.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="66 05")$Prcp, na.rm = T)
ma.67.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="67 05")$Prcp, na.rm = T)
ma.68.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="68 05")$Prcp, na.rm = T)
ma.69.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="69 05")$Prcp, na.rm = T)
ma.70.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="70 05")$Prcp, na.rm = T)
ma.71.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="71 05")$Prcp, na.rm = T)
ma.72.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="72 05")$Prcp, na.rm = T)
ma.73.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="73 05")$Prcp, na.rm = T)
ma.74.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="74 05")$Prcp, na.rm = T)
ma.75.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="75 05")$Prcp, na.rm = T)
ma.76.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="76 05")$Prcp, na.rm = T)
ma.77.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="77 05")$Prcp, na.rm = T)
ma.78.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="78 05")$Prcp, na.rm = T)
ma.79.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="79 05")$Prcp, na.rm = T)
ma.80.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="80 05")$Prcp, na.rm = T)
ma.81.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="81 05")$Prcp, na.rm = T)
ma.82.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="82 05")$Prcp, na.rm = T)
ma.83.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="83 05")$Prcp, na.rm = T)
ma.84.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="84 05")$Prcp, na.rm = T)
ma.85.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="85 05")$Prcp, na.rm = T)
ma.86.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="86 05")$Prcp, na.rm = T)
ma.87.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="87 05")$Prcp, na.rm = T)
ma.88.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="88 05")$Prcp, na.rm = T)
ma.89.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="89 05")$Prcp, na.rm = T)
ma.90.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="90 05")$Prcp, na.rm = T)
ma.91.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="91 05")$Prcp, na.rm = T)
ma.92.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="92 05")$Prcp, na.rm = T)
ma.93.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="93 05")$Prcp, na.rm = T)
ma.94.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="94 05")$Prcp, na.rm = T)
ma.95.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="95 05")$Prcp, na.rm = T)
ma.96.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="96 05")$Prcp, na.rm = T)
ma.97.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="97 05")$Prcp, na.rm = T)
ma.98.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="98 05")$Prcp, na.rm = T)
ma.99.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="99 05")$Prcp, na.rm = T)
ma.00.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="00 05")$Prcp, na.rm = T)
ma.01.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="01 05")$Prcp, na.rm = T)
ma.02.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="02 05")$Prcp, na.rm = T)
ma.03.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="03 05")$Prcp, na.rm = T)
ma.04.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="04 05")$Prcp, na.rm = T)
ma.05.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="05 05")$Prcp, na.rm = T)
ma.06.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="06 05")$Prcp, na.rm = T)
ma.07.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="07 05")$Prcp, na.rm = T)
ma.08.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="08 05")$Prcp, na.rm = T)
ma.09.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="09 05")$Prcp, na.rm = T)
ma.10.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="10 05")$Prcp, na.rm = T)
ma.11.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="11 05")$Prcp, na.rm = T)
ma.12.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="12 05")$Prcp, na.rm = T)
ma.13.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="13 05")$Prcp, na.rm = T)
ma.14.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="14 05")$Prcp, na.rm = T)
ma.15.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="15 05")$Prcp, na.rm = T)
ma.16.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="16 05")$Prcp, na.rm = T)
#f.17.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="17 05")$Prcp, na.rm = T)
#f.18.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="18 01")$Prcp, na.rm = T)
#c.19.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="19 01")$Prcp, na.rm = T)

Mav <- c(ma.60.1,ma.61.1,ma.62.1,ma.63.1,ma.64.1,ma.65.1,ma.66.1,ma.67.1,ma.68.1,ma.69.1,ma.70.1,
          ma.71.1,ma.72.1,ma.73.1,ma.74.1,ma.75.1,ma.76.1,ma.77.1,ma.78.1,ma.79.1,ma.80.1,ma.81.1,ma.82.1,
          ma.83.1,ma.84.1,ma.85.1,ma.86.1,ma.87.1,ma.88.1,ma.89.1,ma.90.1,ma.91.1,ma.92.1,ma.93.1,ma.94.1,
          ma.95.1,ma.96.1,ma.97.1,ma.98.1,ma.99.1,ma.00.1,ma.01.1,ma.02.1,ma.03.1,ma.04.1,ma.05.1,ma.06.1,
          ma.07.1,ma.08.1,ma.09.1,ma.10.1,ma.11.1,ma.12.1,ma.13.1,ma.14.1,ma.15.1,ma.16.1)

ggplot(data=data.frame(Prcp.df.ann[,1], Mav), aes(x=Prcp.df.ann[,1], y=Mav)) + 
  geom_line(col="darkblue", lwd=2) + scale_x_date(date_breaks = "4 years",
                                                  date_labels = "%Y") +
  labs(title = "Seasonal Cycle of May (1960-2016)\n Tamale",
       x="Year",y="Rainfall (mm)") +
  theme(plot.title = element_text(size=18, hjust = 0.5, face = "bold"),
        axis.text = element_text(size = 13),
        axis.title = element_text(size = 13))
dev.copy(png, filename="Seasonal Cycle of May (1960-2016) of Tamale.png", width=1000,
         height=750)
dev.off()



#let,
pprr <- sju
head(pprr)
ju.60.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="60 06")$Prcp, na.rm = T)
ju.61.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="61 06")$Prcp, na.rm = T)
ju.62.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="62 06")$Prcp, na.rm = T)
ju.63.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="63 06")$Prcp, na.rm = T)
ju.64.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="64 06")$Prcp, na.rm = T)
ju.65.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="65 06")$Prcp, na.rm = T)
ju.66.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="66 06")$Prcp, na.rm = T)
ju.67.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="67 06")$Prcp, na.rm = T)
ju.68.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="68 06")$Prcp, na.rm = T)
ju.69.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="69 06")$Prcp, na.rm = T)
ju.70.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="70 06")$Prcp, na.rm = T)
ju.71.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="71 06")$Prcp, na.rm = T)
ju.72.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="72 06")$Prcp, na.rm = T)
ju.73.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="73 06")$Prcp, na.rm = T)
ju.74.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="74 06")$Prcp, na.rm = T)
ju.75.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="75 06")$Prcp, na.rm = T)
ju.76.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="76 06")$Prcp, na.rm = T)
ju.77.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="77 06")$Prcp, na.rm = T)
ju.78.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="78 06")$Prcp, na.rm = T)
ju.79.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="79 06")$Prcp, na.rm = T)
ju.80.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="80 06")$Prcp, na.rm = T)
ju.81.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="81 06")$Prcp, na.rm = T)
ju.82.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="82 06")$Prcp, na.rm = T)
ju.83.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="83 06")$Prcp, na.rm = T)
ju.84.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="84 06")$Prcp, na.rm = T)
ju.85.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="85 06")$Prcp, na.rm = T)
ju.86.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="86 06")$Prcp, na.rm = T)
ju.87.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="87 06")$Prcp, na.rm = T)
ju.88.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="88 06")$Prcp, na.rm = T)
ju.89.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="89 06")$Prcp, na.rm = T)
ju.90.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="90 06")$Prcp, na.rm = T)
ju.91.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="91 06")$Prcp, na.rm = T)
ju.92.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="92 06")$Prcp, na.rm = T)
ju.93.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="93 06")$Prcp, na.rm = T)
ju.94.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="94 06")$Prcp, na.rm = T)
ju.95.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="95 06")$Prcp, na.rm = T)
ju.96.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="96 06")$Prcp, na.rm = T)
ju.97.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="97 06")$Prcp, na.rm = T)
ju.98.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="98 06")$Prcp, na.rm = T)
ju.99.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="99 06")$Prcp, na.rm = T)
ju.00.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="00 06")$Prcp, na.rm = T)
ju.01.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="01 06")$Prcp, na.rm = T)
ju.02.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="02 06")$Prcp, na.rm = T)
ju.03.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="03 06")$Prcp, na.rm = T)
ju.04.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="04 06")$Prcp, na.rm = T)
ju.05.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="05 06")$Prcp, na.rm = T)
ju.06.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="06 06")$Prcp, na.rm = T)
ju.07.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="07 06")$Prcp, na.rm = T)
ju.08.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="08 06")$Prcp, na.rm = T)
ju.09.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="09 06")$Prcp, na.rm = T)
ju.10.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="10 06")$Prcp, na.rm = T)
ju.11.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="11 06")$Prcp, na.rm = T)
ju.12.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="12 06")$Prcp, na.rm = T)
ju.13.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="13 06")$Prcp, na.rm = T)
ju.14.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="14 06")$Prcp, na.rm = T)
ju.15.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="15 06")$Prcp, na.rm = T)
ju.16.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="16 06")$Prcp, na.rm = T)
#f.17.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="17 05")$Prcp, na.rm = T)
#f.18.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="18 01")$Prcp, na.rm = T)
#c.19.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="19 01")$Prcp, na.rm = T)

juv <- c(ju.60.1,ju.61.1,ju.62.1,ju.63.1,ju.64.1,ju.65.1,ju.66.1,ju.67.1,ju.68.1,ju.69.1,ju.70.1,
         ju.71.1,ju.72.1,ju.73.1,ju.74.1,ju.75.1,ju.76.1,ju.77.1,ju.78.1,ju.79.1,ju.80.1,ju.81.1,ju.82.1,
         ju.83.1,ju.84.1,ju.85.1,ju.86.1,ju.87.1,ju.88.1,ju.89.1,ju.90.1,ju.91.1,ju.92.1,ju.93.1,ju.94.1,
         ju.95.1,ju.96.1,ju.97.1,ju.98.1,ju.99.1,ju.00.1,ju.01.1,ju.02.1,ju.03.1,ju.04.1,ju.05.1,ju.06.1,
         ju.07.1,ju.08.1,ju.09.1,ju.10.1,ju.11.1,ju.12.1,ju.13.1,ju.14.1,ju.15.1,ju.16.1)

ggplot(data=data.frame(Prcp.df.ann[,1], juv), aes(x=Prcp.df.ann[,1], y=juv)) + 
  geom_line(col="darkblue", lwd=2) + scale_x_date(date_breaks = "4 years",
                                                  date_labels = "%Y") +
  labs(title = "Seasonal Cycle of June (1960-2016)\n Tamale",
       x="Year",y="Rainfall (mm)") +
  theme(plot.title = element_text(size=18, hjust = 0.5, face = "bold"),
        axis.text = element_text(size = 13),
        axis.title = element_text(size = 13))
dev.copy(png, filename="Seasonal Cycle of June (1960-2016) of Tamale.png", width=1000,
         height=750)
dev.off()



#let,
pprr <- sjl
head(pprr)
jl.60.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="60 07")$Prcp, na.rm = T)
jl.61.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="61 07")$Prcp, na.rm = T)
jl.62.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="62 07")$Prcp, na.rm = T)
jl.63.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="63 07")$Prcp, na.rm = T)
jl.64.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="64 07")$Prcp, na.rm = T)
jl.65.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="65 07")$Prcp, na.rm = T)
jl.66.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="66 07")$Prcp, na.rm = T)
jl.67.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="67 07")$Prcp, na.rm = T)
jl.68.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="68 07")$Prcp, na.rm = T)
jl.69.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="69 07")$Prcp, na.rm = T)
jl.70.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="70 07")$Prcp, na.rm = T)
jl.71.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="71 07")$Prcp, na.rm = T)
jl.72.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="72 07")$Prcp, na.rm = T)
jl.73.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="73 07")$Prcp, na.rm = T)
jl.74.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="74 07")$Prcp, na.rm = T)
jl.75.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="75 07")$Prcp, na.rm = T)
jl.76.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="76 07")$Prcp, na.rm = T)
jl.77.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="77 07")$Prcp, na.rm = T)
jl.78.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="78 07")$Prcp, na.rm = T)
jl.79.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="79 07")$Prcp, na.rm = T)
jl.80.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="80 07")$Prcp, na.rm = T)
jl.81.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="81 07")$Prcp, na.rm = T)
jl.82.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="82 07")$Prcp, na.rm = T)
jl.83.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="83 07")$Prcp, na.rm = T)
jl.84.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="84 07")$Prcp, na.rm = T)
jl.85.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="85 07")$Prcp, na.rm = T)
jl.86.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="86 07")$Prcp, na.rm = T)
jl.87.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="87 07")$Prcp, na.rm = T)
jl.88.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="88 07")$Prcp, na.rm = T)
jl.89.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="89 07")$Prcp, na.rm = T)
jl.90.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="90 07")$Prcp, na.rm = T)
jl.91.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="91 07")$Prcp, na.rm = T)
jl.92.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="92 07")$Prcp, na.rm = T)
jl.93.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="93 07")$Prcp, na.rm = T)
jl.94.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="94 07")$Prcp, na.rm = T)
jl.95.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="95 07")$Prcp, na.rm = T)
jl.96.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="96 07")$Prcp, na.rm = T)
jl.97.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="97 07")$Prcp, na.rm = T)
jl.98.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="98 07")$Prcp, na.rm = T)
jl.99.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="99 07")$Prcp, na.rm = T)
jl.00.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="00 07")$Prcp, na.rm = T)
jl.01.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="01 07")$Prcp, na.rm = T)
jl.02.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="02 07")$Prcp, na.rm = T)
jl.03.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="03 07")$Prcp, na.rm = T)
jl.04.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="04 07")$Prcp, na.rm = T)
jl.05.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="05 07")$Prcp, na.rm = T)
jl.06.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="06 07")$Prcp, na.rm = T)
jl.07.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="07 07")$Prcp, na.rm = T)
jl.08.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="08 07")$Prcp, na.rm = T)
jl.09.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="09 07")$Prcp, na.rm = T)
jl.10.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="10 07")$Prcp, na.rm = T)
jl.11.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="11 07")$Prcp, na.rm = T)
jl.12.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="12 07")$Prcp, na.rm = T)
jl.13.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="13 07")$Prcp, na.rm = T)
jl.14.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="14 07")$Prcp, na.rm = T)
jl.15.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="15 07")$Prcp, na.rm = T)
jl.16.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="16 07")$Prcp, na.rm = T)
#f.17.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="17 05")$Prcp, na.rm = T)
#f.18.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="18 01")$Prcp, na.rm = T)
#c.19.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="19 01")$Prcp, na.rm = T)

jlv <- c(jl.60.1,jl.61.1,jl.62.1,jl.63.1,jl.64.1,jl.65.1,jl.66.1,jl.67.1,jl.68.1,jl.69.1,jl.70.1,
         jl.71.1,jl.72.1,jl.73.1,jl.74.1,jl.75.1,jl.76.1,jl.77.1,jl.78.1,jl.79.1,jl.80.1,jl.81.1,jl.82.1,
         jl.83.1,jl.84.1,jl.85.1,jl.86.1,jl.87.1,jl.88.1,jl.89.1,jl.90.1,jl.91.1,jl.92.1,jl.93.1,jl.94.1,
         jl.95.1,jl.96.1,jl.97.1,jl.98.1,jl.99.1,jl.00.1,jl.01.1,jl.02.1,jl.03.1,jl.04.1,jl.05.1,jl.06.1,
         jl.07.1,jl.08.1,jl.09.1,jl.10.1,jl.11.1,jl.12.1,jl.13.1,jl.14.1,jl.15.1,jl.16.1)

ggplot(data=data.frame(Prcp.df.ann[,1], jlv), aes(x=Prcp.df.ann[,1], y=jlv)) + 
  geom_line(col="darkblue", lwd=2) + scale_x_date(date_breaks = "4 years",
                                                  date_labels = "%Y") +
  labs(title = "Seasonal Cycle of July (1960-2016)\n Tamale",
       x="Year",y="Rainfall (mm)") +
  theme(plot.title = element_text(size=18, hjust = 0.5, face = "bold"),
        axis.text = element_text(size = 13),
        axis.title = element_text(size = 13))
dev.copy(png, filename="Seasonal Cycle of July (1960-2016) of Tamale.png",
         width=1000, height=750)
dev.off()




#let,
pprr <- sau
head(pprr)
au.60.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="60 08")$Prcp, na.rm = T)
au.61.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="61 08")$Prcp, na.rm = T)
au.62.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="62 08")$Prcp, na.rm = T)
au.63.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="63 08")$Prcp, na.rm = T)
au.64.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="64 08")$Prcp, na.rm = T)
au.65.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="65 08")$Prcp, na.rm = T)
au.66.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="66 08")$Prcp, na.rm = T)
au.67.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="67 08")$Prcp, na.rm = T)
au.68.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="68 08")$Prcp, na.rm = T)
au.69.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="69 08")$Prcp, na.rm = T)
au.70.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="70 08")$Prcp, na.rm = T)
au.71.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="71 08")$Prcp, na.rm = T)
au.72.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="72 08")$Prcp, na.rm = T)
au.73.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="73 08")$Prcp, na.rm = T)
au.74.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="74 08")$Prcp, na.rm = T)
au.75.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="75 08")$Prcp, na.rm = T)
au.76.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="76 08")$Prcp, na.rm = T)
au.77.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="77 08")$Prcp, na.rm = T)
au.78.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="78 08")$Prcp, na.rm = T)
au.79.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="79 08")$Prcp, na.rm = T)
au.80.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="80 08")$Prcp, na.rm = T)
au.81.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="81 08")$Prcp, na.rm = T)
au.82.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="82 08")$Prcp, na.rm = T)
au.83.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="83 08")$Prcp, na.rm = T)
au.84.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="84 08")$Prcp, na.rm = T)
au.85.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="85 08")$Prcp, na.rm = T)
au.86.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="86 08")$Prcp, na.rm = T)
au.87.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="87 08")$Prcp, na.rm = T)
au.88.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="88 08")$Prcp, na.rm = T)
au.89.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="89 08")$Prcp, na.rm = T)
au.90.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="90 08")$Prcp, na.rm = T)
au.91.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="91 08")$Prcp, na.rm = T)
au.92.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="92 08")$Prcp, na.rm = T)
au.93.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="93 08")$Prcp, na.rm = T)
au.94.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="94 08")$Prcp, na.rm = T)
au.95.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="95 08")$Prcp, na.rm = T)
au.96.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="96 08")$Prcp, na.rm = T)
au.97.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="97 08")$Prcp, na.rm = T)
au.98.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="98 08")$Prcp, na.rm = T)
au.99.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="99 08")$Prcp, na.rm = T)
au.00.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="00 08")$Prcp, na.rm = T)
au.01.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="01 08")$Prcp, na.rm = T)
au.02.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="02 08")$Prcp, na.rm = T)
au.03.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="03 08")$Prcp, na.rm = T)
au.04.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="04 08")$Prcp, na.rm = T)
au.05.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="05 08")$Prcp, na.rm = T)
au.06.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="06 08")$Prcp, na.rm = T)
au.07.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="07 08")$Prcp, na.rm = T)
au.08.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="08 08")$Prcp, na.rm = T)
au.09.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="09 08")$Prcp, na.rm = T)
au.10.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="10 08")$Prcp, na.rm = T)
au.11.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="11 08")$Prcp, na.rm = T)
au.12.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="12 08")$Prcp, na.rm = T)
au.13.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="13 08")$Prcp, na.rm = T)
au.14.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="14 08")$Prcp, na.rm = T)
au.15.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="15 08")$Prcp, na.rm = T)
au.16.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="16 08")$Prcp, na.rm = T)
#f.17.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="17 05")$Prcp, na.rm = T)
#f.18.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="18 01")$Prcp, na.rm = T)
#c.19.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="19 01")$Prcp, na.rm = T)

auv <- c(au.60.1,au.61.1,au.62.1,au.63.1,au.64.1,au.65.1,au.66.1,au.67.1,au.68.1,au.69.1,au.70.1,
         au.71.1,au.72.1,au.73.1,au.74.1,au.75.1,au.76.1,au.77.1,au.78.1,au.79.1,au.80.1,au.81.1,au.82.1,
         au.83.1,au.84.1,au.85.1,au.86.1,au.87.1,au.88.1,au.89.1,au.90.1,au.91.1,au.92.1,au.93.1,au.94.1,
         au.95.1,au.96.1,au.97.1,au.98.1,au.99.1,au.00.1,au.01.1,au.02.1,au.03.1,au.04.1,au.05.1,au.06.1,
         au.07.1,au.08.1,au.09.1,au.10.1,au.11.1,au.12.1,au.13.1,au.14.1,au.15.1,au.16.1)

ggplot(data=data.frame(Prcp.df.ann[,1], auv), aes(x=Prcp.df.ann[,1], y=auv)) + 
  geom_line(col="darkblue", lwd=2) + scale_x_date(date_breaks = "4 years",
                                                  date_labels = "%Y") +
  labs(title = "Seasonal Cycle of August (1960-2016)\n Tamale",
       x="Year",y="Rainfall (mm)") +
  theme(plot.title = element_text(size=18, hjust = 0.5, face = "bold"),
        axis.text = element_text(size = 13),
        axis.title = element_text(size = 13))
dev.copy(png, filename="Seasonal Cycle of August (1960-2016) of Tamale.png",
         width=1000, height=750)
dev.off()



#September
#let,
pprr <- ss
head(pprr)
s.60.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="60 09")$Prcp, na.rm = T)
s.61.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="61 09")$Prcp, na.rm = T)
s.62.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="62 09")$Prcp, na.rm = T)
s.63.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="63 09")$Prcp, na.rm = T)
s.64.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="64 09")$Prcp, na.rm = T)
s.65.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="65 09")$Prcp, na.rm = T)
s.66.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="66 09")$Prcp, na.rm = T)
s.67.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="67 09")$Prcp, na.rm = T)
s.68.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="68 09")$Prcp, na.rm = T)
s.69.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="69 09")$Prcp, na.rm = T)
s.70.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="70 09")$Prcp, na.rm = T)
s.71.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="71 09")$Prcp, na.rm = T)
s.72.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="72 09")$Prcp, na.rm = T)
s.73.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="73 09")$Prcp, na.rm = T)
s.74.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="74 09")$Prcp, na.rm = T)
s.75.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="75 09")$Prcp, na.rm = T)
s.76.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="76 09")$Prcp, na.rm = T)
s.77.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="77 09")$Prcp, na.rm = T)
s.78.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="78 09")$Prcp, na.rm = T)
s.79.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="79 09")$Prcp, na.rm = T)
s.80.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="80 09")$Prcp, na.rm = T)
s.81.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="81 09")$Prcp, na.rm = T)
s.82.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="82 09")$Prcp, na.rm = T)
s.83.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="83 09")$Prcp, na.rm = T)
s.84.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="84 09")$Prcp, na.rm = T)
s.85.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="85 09")$Prcp, na.rm = T)
s.86.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="86 09")$Prcp, na.rm = T)
s.87.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="87 09")$Prcp, na.rm = T)
s.88.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="88 09")$Prcp, na.rm = T)
s.89.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="89 09")$Prcp, na.rm = T)
s.90.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="90 09")$Prcp, na.rm = T)
s.91.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="91 09")$Prcp, na.rm = T)
s.92.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="92 09")$Prcp, na.rm = T)
s.93.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="93 09")$Prcp, na.rm = T)
s.94.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="94 09")$Prcp, na.rm = T)
s.95.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="95 09")$Prcp, na.rm = T)
s.96.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="96 09")$Prcp, na.rm = T)
s.97.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="97 09")$Prcp, na.rm = T)
s.98.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="98 09")$Prcp, na.rm = T)
s.99.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="99 09")$Prcp, na.rm = T)
s.00.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="00 09")$Prcp, na.rm = T)
s.01.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="01 09")$Prcp, na.rm = T)
s.02.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="02 09")$Prcp, na.rm = T)
s.03.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="03 09")$Prcp, na.rm = T)
s.04.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="04 09")$Prcp, na.rm = T)
s.05.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="05 09")$Prcp, na.rm = T)
s.06.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="06 09")$Prcp, na.rm = T)
s.07.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="07 09")$Prcp, na.rm = T)
s.08.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="08 09")$Prcp, na.rm = T)
s.09.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="09 09")$Prcp, na.rm = T)
s.10.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="10 09")$Prcp, na.rm = T)
s.11.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="11 09")$Prcp, na.rm = T)
s.12.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="12 09")$Prcp, na.rm = T)
s.13.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="13 09")$Prcp, na.rm = T)
s.14.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="14 09")$Prcp, na.rm = T)
s.15.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="15 09")$Prcp, na.rm = T)
s.16.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="16 09")$Prcp, na.rm = T)
#f.17.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="17 05")$Prcp, na.rm = T)
#f.18.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="18 01")$Prcp, na.rm = T)
#c.19.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="19 01")$Prcp, na.rm = T)

sv <- c(s.60.1,s.61.1,s.62.1,s.63.1,s.64.1,s.65.1,s.66.1,s.67.1,s.68.1,s.69.1,s.70.1,
         s.71.1,s.72.1,s.73.1,s.74.1,s.75.1,s.76.1,s.77.1,s.78.1,s.79.1,s.80.1,s.81.1,s.82.1,
         s.83.1,s.84.1,s.85.1,s.86.1,s.87.1,s.88.1,s.89.1,s.90.1,s.91.1,s.92.1,s.93.1,s.94.1,
         s.95.1,s.96.1,s.97.1,s.98.1,s.99.1,s.00.1,s.01.1,s.02.1,s.03.1,s.04.1,s.05.1,s.06.1,
         s.07.1,s.08.1,s.09.1,s.10.1,s.11.1,s.12.1,s.13.1,s.14.1,s.15.1,s.16.1)

ggplot(data=data.frame(Prcp.df.ann[,1], Mav), aes(x=Prcp.df.ann[,1], y=Mav)) + 
  geom_line(col="darkblue", lwd=2) + scale_x_date(date_breaks = "4 years",
                                                  date_labels = "%Y") +
  labs(title = "Seasonal Cycle of September (1960-2016)\n Tamale",
       x="Year",y="Rainfall (mm)") +
  theme(plot.title = element_text(size=18, hjust = 0.5, face = "bold"),
        axis.text = element_text(size = 13),
        axis.title = element_text(size = 13))
dev.copy(png, filename="Seasonal Cycle of September (1960-2016) of Tamale.png", width = 1000,
         height=750)
dev.off()



pprr <- sn
head(pprr)
n.60.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="60 11")$Prcp, na.rm = T)
n.61.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="61 11")$Prcp, na.rm = T)
n.62.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="62 11")$Prcp, na.rm = T)
n.63.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="63 11")$Prcp, na.rm = T)
n.64.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="64 11")$Prcp, na.rm = T)
n.65.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="65 11")$Prcp, na.rm = T)
n.66.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="66 11")$Prcp, na.rm = T)
n.67.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="67 11")$Prcp, na.rm = T)
n.68.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="68 11")$Prcp, na.rm = T)
n.69.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="69 11")$Prcp, na.rm = T)
n.70.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="70 11")$Prcp, na.rm = T)
n.71.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="71 11")$Prcp, na.rm = T)
n.72.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="72 11")$Prcp, na.rm = T)
n.73.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="73 11")$Prcp, na.rm = T)
n.74.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="74 11")$Prcp, na.rm = T)
n.75.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="75 11")$Prcp, na.rm = T)
n.76.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="76 11")$Prcp, na.rm = T)
n.77.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="77 11")$Prcp, na.rm = T)
n.78.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="78 11")$Prcp, na.rm = T)
n.79.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="79 11")$Prcp, na.rm = T)
n.80.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="80 11")$Prcp, na.rm = T)
n.81.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="81 11")$Prcp, na.rm = T)
n.82.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="82 11")$Prcp, na.rm = T)
n.83.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="83 11")$Prcp, na.rm = T)
n.84.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="84 11")$Prcp, na.rm = T)
n.85.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="85 11")$Prcp, na.rm = T)
n.86.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="86 11")$Prcp, na.rm = T)
n.87.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="87 11")$Prcp, na.rm = T)
n.88.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="88 11")$Prcp, na.rm = T)
n.89.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="89 11")$Prcp, na.rm = T)
n.90.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="90 11")$Prcp, na.rm = T)
n.91.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="91 11")$Prcp, na.rm = T)
n.92.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="92 11")$Prcp, na.rm = T)
n.93.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="93 11")$Prcp, na.rm = T)
n.94.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="94 11")$Prcp, na.rm = T)
n.95.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="95 11")$Prcp, na.rm = T)
n.96.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="96 11")$Prcp, na.rm = T)
n.97.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="97 11")$Prcp, na.rm = T)
n.98.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="98 11")$Prcp, na.rm = T)
n.99.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="99 11")$Prcp, na.rm = T)
n.00.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="00 11")$Prcp, na.rm = T)
n.01.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="01 11")$Prcp, na.rm = T)
n.02.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="02 11")$Prcp, na.rm = T)
n.03.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="03 11")$Prcp, na.rm = T)
n.04.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="04 11")$Prcp, na.rm = T)
n.05.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="05 11")$Prcp, na.rm = T)
n.06.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="06 11")$Prcp, na.rm = T)
n.07.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="07 11")$Prcp, na.rm = T)
n.08.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="08 11")$Prcp, na.rm = T)
n.09.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="09 11")$Prcp, na.rm = T)
n.10.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="10 11")$Prcp, na.rm = T)
n.11.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="11 11")$Prcp, na.rm = T)
n.12.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="12 11")$Prcp, na.rm = T)
n.13.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="13 11")$Prcp, na.rm = T)
n.14.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="14 11")$Prcp, na.rm = T)
n.15.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="15 11")$Prcp, na.rm = T)
n.16.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="16 11")$Prcp, na.rm = T)
#f.17.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="17 05")$Prcp, na.rm = T)
#f.18.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="18 01")$Prcp, na.rm = T)
#c.19.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="19 01")$Prcp, na.rm = T)

nv <- c(n.60.1,n.61.1,n.62.1,n.63.1,n.64.1,n.65.1,n.66.1,n.67.1,n.68.1,n.69.1,n.70.1,
        n.71.1,n.72.1,n.73.1,n.74.1,n.75.1,n.76.1,n.77.1,n.78.1,n.79.1,n.80.1,n.81.1,n.82.1,
        n.83.1,n.84.1,n.85.1,n.86.1,n.87.1,n.88.1,n.89.1,n.90.1,n.91.1,n.92.1,n.93.1,n.94.1,
        n.95.1,n.96.1,n.97.1,n.98.1,n.99.1,n.00.1,n.01.1,n.02.1,n.03.1,n.04.1,n.05.1,n.06.1,
        n.07.1,n.08.1,n.09.1,n.10.1,n.11.1,n.12.1,n.13.1,n.14.1,n.15.1,n.16.1)

ggplot(data=data.frame(Prcp.df.ann[,1], nv), aes(x=Prcp.df.ann[,1], y=nv)) + 
  geom_line(col="darkblue", lwd=2) + scale_x_date(date_breaks = "4 years",
                                                  date_labels = "%Y") +
  labs(title = "Seasonal Cycle of November (1960-2016)\n Tamale",
       x="Year",y="Rainfall (mm)") +
  theme(plot.title = element_text(size=18, hjust = 0.5, face = "bold"),
        axis.text = element_text(size = 13),
        axis.title = element_text(size = 13))
dev.copy(png, filename="Seasonal Cycle of November (1960-2016) of Tamale.png", width = 1000,
         height=750)
dev.off()


#October
#let,
pprr <- so
head(pprr)
o.60.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="60 10")$Prcp, na.rm = T)
o.61.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="61 10")$Prcp, na.rm = T)
o.62.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="62 10")$Prcp, na.rm = T)
o.63.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="63 10")$Prcp, na.rm = T)
o.64.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="64 10")$Prcp, na.rm = T)
o.65.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="65 10")$Prcp, na.rm = T)
o.66.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="66 10")$Prcp, na.rm = T)
o.67.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="67 10")$Prcp, na.rm = T)
o.68.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="68 10")$Prcp, na.rm = T)
o.69.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="69 10")$Prcp, na.rm = T)
o.70.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="70 10")$Prcp, na.rm = T)
o.71.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="71 10")$Prcp, na.rm = T)
o.72.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="72 10")$Prcp, na.rm = T)
o.73.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="73 10")$Prcp, na.rm = T)
o.74.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="74 10")$Prcp, na.rm = T)
o.75.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="75 10")$Prcp, na.rm = T)
o.76.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="76 10")$Prcp, na.rm = T)
o.77.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="77 10")$Prcp, na.rm = T)
o.78.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="78 10")$Prcp, na.rm = T)
o.79.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="79 10")$Prcp, na.rm = T)
o.80.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="80 10")$Prcp, na.rm = T)
o.81.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="81 10")$Prcp, na.rm = T)
o.82.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="82 10")$Prcp, na.rm = T)
o.83.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="83 10")$Prcp, na.rm = T)
o.84.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="84 10")$Prcp, na.rm = T)
o.85.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="85 10")$Prcp, na.rm = T)
o.86.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="86 10")$Prcp, na.rm = T)
o.87.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="87 10")$Prcp, na.rm = T)
o.88.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="88 10")$Prcp, na.rm = T)
o.89.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="89 10")$Prcp, na.rm = T)
o.90.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="90 10")$Prcp, na.rm = T)
o.91.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="91 10")$Prcp, na.rm = T)
o.92.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="92 10")$Prcp, na.rm = T)
o.93.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="93 10")$Prcp, na.rm = T)
o.94.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="94 10")$Prcp, na.rm = T)
o.95.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="95 10")$Prcp, na.rm = T)
o.96.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="96 10")$Prcp, na.rm = T)
o.97.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="97 10")$Prcp, na.rm = T)
o.98.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="98 10")$Prcp, na.rm = T)
o.99.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="99 10")$Prcp, na.rm = T)
o.00.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="00 10")$Prcp, na.rm = T)
o.01.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="01 10")$Prcp, na.rm = T)
o.02.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="02 10")$Prcp, na.rm = T)
o.03.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="03 10")$Prcp, na.rm = T)
o.04.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="04 10")$Prcp, na.rm = T)
o.05.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="05 10")$Prcp, na.rm = T)
o.06.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="06 10")$Prcp, na.rm = T)
o.07.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="07 10")$Prcp, na.rm = T)
o.08.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="08 10")$Prcp, na.rm = T)
o.09.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="09 10")$Prcp, na.rm = T)
o.10.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="10 10")$Prcp, na.rm = T)
o.11.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="11 10")$Prcp, na.rm = T)
o.12.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="12 10")$Prcp, na.rm = T)
o.13.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="13 10")$Prcp, na.rm = T)
o.14.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="14 10")$Prcp, na.rm = T)
o.15.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="15 10")$Prcp, na.rm = T)
o.16.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="16 10")$Prcp, na.rm = T)
#f.17.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="17 05")$Prcp, na.rm = T)
#f.18.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="18 01")$Prcp, na.rm = T)
#c.19.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="19 01")$Prcp, na.rm = T)

ov <- c(o.60.1,o.61.1,o.62.1,o.63.1,o.64.1,o.65.1,o.66.1,o.67.1,o.68.1,o.69.1,o.70.1,
         o.71.1,o.72.1,o.73.1,o.74.1,o.75.1,o.76.1,o.77.1,o.78.1,o.79.1,o.80.1,o.81.1,o.82.1,
         o.83.1,o.84.1,o.85.1,o.86.1,o.87.1,o.88.1,o.89.1,o.90.1,o.91.1,o.92.1,o.93.1,o.94.1,
         o.95.1,o.96.1,o.97.1,o.98.1,o.99.1,o.00.1,o.01.1,o.02.1,o.03.1,o.04.1,o.05.1,o.06.1,
         o.07.1,o.08.1,o.09.1,o.10.1,o.11.1,o.12.1,o.13.1,o.14.1,o.15.1,o.16.1)

ggplot(data=data.frame(Prcp.df.ann[,1], ov), aes(x=Prcp.df.ann[,1], y=ov)) + 
  geom_line(col="darkblue", lwd=2) + scale_x_date(date_breaks = "4 years",
                                                  date_labels = "%Y") +
  labs(title = "Seasonal Cycle of October (1960-2016)\n Tamale",
       x="Year",y="Rainfall (mm)") +
  theme(plot.title = element_text(size=18, hjust = 0.5, face = "bold"),
        axis.text = element_text(size = 13),
        axis.title = element_text(size = 13))
dev.copy(png, filename="Seasonal Cycle of October (1960-2016) of Tamale.png",
         width=1000, height=750)
dev.off()



#October
#let,
pprr <- sd
head(pprr)
d.60.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="60 12")$Prcp, na.rm = T)
d.61.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="61 12")$Prcp, na.rm = T)
d.62.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="62 12")$Prcp, na.rm = T)
d.63.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="63 12")$Prcp, na.rm = T)
d.64.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="64 12")$Prcp, na.rm = T)
d.65.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="65 12")$Prcp, na.rm = T)
d.66.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="66 12")$Prcp, na.rm = T)
d.67.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="67 12")$Prcp, na.rm = T)
d.68.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="68 12")$Prcp, na.rm = T)
d.69.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="69 12")$Prcp, na.rm = T)
d.70.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="70 12")$Prcp, na.rm = T)
d.71.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="71 12")$Prcp, na.rm = T)
d.72.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="72 12")$Prcp, na.rm = T)
d.73.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="73 12")$Prcp, na.rm = T)
d.74.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="74 12")$Prcp, na.rm = T)
d.75.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="75 12")$Prcp, na.rm = T)
d.76.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="76 12")$Prcp, na.rm = T)
d.77.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="77 12")$Prcp, na.rm = T)
d.78.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="78 12")$Prcp, na.rm = T)
d.79.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="79 12")$Prcp, na.rm = T)
d.80.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="80 12")$Prcp, na.rm = T)
d.81.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="81 12")$Prcp, na.rm = T)
d.82.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="82 12")$Prcp, na.rm = T)
d.83.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="83 12")$Prcp, na.rm = T)
d.84.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="84 12")$Prcp, na.rm = T)
d.85.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="85 12")$Prcp, na.rm = T)
d.86.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="86 12")$Prcp, na.rm = T)
d.87.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="87 12")$Prcp, na.rm = T)
d.88.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="88 12")$Prcp, na.rm = T)
d.89.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="89 12")$Prcp, na.rm = T)
d.90.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="90 12")$Prcp, na.rm = T)
d.91.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="91 12")$Prcp, na.rm = T)
d.92.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="92 12")$Prcp, na.rm = T)
d.93.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="93 12")$Prcp, na.rm = T)
d.94.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="94 12")$Prcp, na.rm = T)
d.95.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="95 12")$Prcp, na.rm = T)
d.96.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="96 12")$Prcp, na.rm = T)
d.97.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="97 12")$Prcp, na.rm = T)
d.98.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="98 12")$Prcp, na.rm = T)
d.99.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="99 12")$Prcp, na.rm = T)
d.00.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="00 12")$Prcp, na.rm = T)
d.01.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="01 12")$Prcp, na.rm = T)
d.02.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="02 12")$Prcp, na.rm = T)
d.03.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="03 12")$Prcp, na.rm = T)
d.04.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="04 12")$Prcp, na.rm = T)
d.05.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="05 12")$Prcp, na.rm = T)
d.06.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="06 12")$Prcp, na.rm = T)
d.07.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="07 12")$Prcp, na.rm = T)
d.08.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="08 12")$Prcp, na.rm = T)
d.09.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="09 12")$Prcp, na.rm = T)
d.10.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="10 12")$Prcp, na.rm = T)
d.11.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="11 12")$Prcp, na.rm = T)
d.12.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="12 12")$Prcp, na.rm = T)
d.13.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="13 12")$Prcp, na.rm = T)
d.14.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="14 12")$Prcp, na.rm = T)
d.15.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="15 12")$Prcp, na.rm = T)
d.16.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="16 12")$Prcp, na.rm = T)
#f.17.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="17 01")$Prcp, na.rm = T)
#f.18.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="18 01")$Prcp, na.rm = T)
#c.19.1 <- mean(subset(pprr, format.Date(pprr$dates, "%y %m")=="19 01")$Prcp, na.rm = T)

dv <- c(d.60.1,d.61.1,d.62.1,d.63.1,d.64.1,d.65.1,d.66.1,d.67.1,d.68.1,d.69.1,d.70.1,
        d.71.1,d.72.1,d.73.1,d.74.1,d.75.1,d.76.1,d.77.1,d.78.1,d.79.1,d.80.1,d.81.1,d.82.1,
        d.83.1,d.84.1,d.85.1,d.86.1,d.87.1,d.88.1,d.89.1,d.90.1,d.91.1,d.92.1,d.93.1,d.94.1,
        d.95.1,d.96.1,d.97.1,d.98.1,d.99.1,d.00.1,d.01.1,d.02.1,d.03.1,d.04.1,d.05.1,d.06.1,
        d.07.1,d.08.1,d.09.1,d.10.1,d.11.1,d.12.1,d.13.1,d.14.1,d.15.1,d.16.1)

ggplot(data=data.frame(Prcp.df.ann[,1], dv), aes(x=Prcp.df.ann[,1], y=dv)) + 
  geom_line(col="darkblue", lwd=2) + scale_x_date(date_breaks = "4 years",
                                                  date_labels = "%Y") +
  labs(title = "Seasonal Cycle of December (1960-2016)\n Tamale",
       x="Year",y="Rainfall (mm)") +
  theme(plot.title = element_text(size=18, hjust = 0.5, face = "bold"),
        axis.text = element_text(size = 13),
        axis.title = element_text(size = 13))
dev.copy(png, filename="Seasonal Cycle of December (1960-2016) of Tamale.png",
         width=1000, height=750)
dev.off()

